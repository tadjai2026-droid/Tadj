# Superseded

This document is retained only as historical documentation. The active V1.6 architecture uses the real GPU Worker for media inference. Payment integration is intentionally deferred until media validation and will use Lemon Squeezy, not the previous payment provider.
